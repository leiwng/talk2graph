# talk2graph — 项目状态

> 最后更新：2026-06-26

## 当前进展

- [x] 产品需求文档
- [x] 技术方案确定
- [x] MVP 开发（Node 服务 + 静态前端）
- [ ] 腾讯云 COS 资源发布（当前默认域名可访问但仍会被强制下载，且线上首页尚未包含本地最新数据集一键示例；需重新发布或改用 Node 公网 URL）
- [x] GitHub 仓库
- [x] 可演示版本验证（内置模板、静态 fallback、API、产品场景自动测试、测试数据集覆盖率评估）
- [ ] 第一批种子用户试用
- [ ] 产品形态决策（Web / 小程序 / App）

## 线上地址

```
https://talk2graph-1259138134.cos-website.ap-guangzhou.myqcloud.com
```

> 2026-06-26 复验：该地址 HTTP 200，不再包含浏览器 API Key 配置，已加载 `src/graph-engine.js`，并提供首页一键示例和角 A/B/C 标注修改入口。但腾讯云 COS 默认静态网站域名返回 `Content-Disposition: attachment` 和 `x-cos-force-download: true`，浏览器会下载页面而不是直接打开；同时线上首页尚未包含本地 `index.html` 中新增的数据集支撑一键示例。本轮 `npm run verify:deployment` 仍失败在 `homepage opens inline instead of downloading` 和 `homepage exposes dataset-backed examples`。
>
> 复验命令：`npm run verify:deployment`。该命令会检查首页是否能内联打开、是否移除浏览器 API Key 配置、是否引用新版静态脚本、是否包含一键示例、数据集支撑示例和角标注入口，以及 `/src/graph-engine.js` 是否可访问。验证自定义域名或 Node 公网服务时可设置 `TALK2GRAPH_DEPLOY_URL=https://your-domain.example npm run verify:deployment`。

## 仓库

```
https://github.com/leiwng/talk2graph
```

## 待解决

### 阻塞

- [ ] COS 默认静态网站域名强制下载页面；需要绑定自定义域名并将源站配置为静态网站源站，或改用 Node 服务 URL 做手工验收。
- [ ] COS 线上首页落后于本地静态页；`npm run verify:deployment` 当前失败在 `homepage opens inline instead of downloading` 和 `homepage exposes dataset-backed examples`。发布脚本已补 `Content-Disposition: inline`、`Cache-Control: no-cache, no-store, must-revalidate` 和 `--prompt-credentials` 隐藏输入模式，仍需交互输入凭据重新发布后复验真实响应头。

### 高优先级

- [ ] 第一次真实用户测试（Node 服务 URL，服务端配置 DeepSeek/OpenAI 兼容 API）
- [ ] 配置可浏览器预览的演示域名（自定义域名或 Node 服务 URL）
- [ ] 重新发布 COS 静态演示页并复验首页内容、`src/graph-engine.js`、一键模板生成和数据集支撑示例
- [x] 新版 COS 静态页复验脚本（`npm run verify:deployment`，可判断线上是否仍是旧版）
- [x] 验证多轮修改的一致性（核心模板通过自动测试验证坐标保持不变，覆盖任意直角边模板的边长移除/斜边高、直角标记外移、辅助线、角标注、坐标范围；测试数据集 #34-38 已全部命中）
- [x] 常见数学图形覆盖度评估（已覆盖需求示例、基础/中等/困难几何、函数图像、坐标几何、参数椭圆、内切圆、交点区域、长方体截面、统计图表）
- [x] 测试数据集自动覆盖率脚本（`npm run evaluate:dataset`；当前内置模板精确覆盖 38/38，单轮 33/33，多轮 5/5，语义误匹配 0）
- [x] 部署复验覆盖新增一键示例（`npm run verify:deployment` 会检查基础几何、单函数、参数椭圆、通用柱状图入口，避免线上仍是旧首页但被误判通过）
- [x] Node 服务 happy path 复验脚本（`npm run verify:service` 检查首页、`/healthz`、内置模板 `/api/generate`、SVG 预览和 TikZ 导出；2026-06-26 本地 `http://127.0.0.1:3002` 验证通过；公网 Node URL 可用 `TALK2GRAPH_SERVICE_URL=...` 验证）
- [x] Node 服务复验已覆盖直角三角形直径圆题图模板，可识别公网服务仍加载旧代码、关键题图落到 LLM 兜底的情况。
- [x] Node 服务容器部署入口（`Dockerfile` + `.dockerignore` + `docs/Node服务部署.md`，用于发布公网 Node URL 后跑 `npm run verify:service`）
- [x] Node 服务非 Docker 发布包入口（2026-06-26 已重新运行 `npm run package:service` 生成 `dist/talk2graph-service.tar.gz`，只包含运行必需文件和 systemd/nginx 模板，适合上传到 CVM/Lighthouse 后作为系统服务运行）
- [x] Node 服务健康检查包含版本和提交号；非 Docker 发布包会写入 `build-info.json`，`npm run verify:service` 会拒绝 `unknown` 版本信息，便于确认公网进程是否已经加载最新包。
- [x] CVM 安装脚本已纳入发布包（`deploy/install-cvm.sh`），服务器侧可直接从 tar 包执行安装，自动解压、安装依赖、安装 systemd、重启并本机校验 `/healthz` 提交号和关键模板，降低旧进程/旧目录误部署风险。
- [x] 渲染错误时的用户反馈（服务端返回渲染警告时前端展示中文提示，并保留 TikZ 导出）

### 中优先级

- [x] 移除浏览器端 TikZJax 占位 bundle（静态模板直接返回 SVG；模板外图形由 Node 服务端尝试渲染，失败时保留 TikZ 导出）
- [x] 预设模板（直角三角形、函数图像、圆内接正多边形、等腰三角形、交点阴影区域、长方体截面、条形统计图）
- [x] 基础移动端适配（小屏动态视口、粘性输入区、预览区和导出按钮换行；仍需真机验证）
- [ ] 自定义域名

## 技术决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 06-24 | TikZ 而非 SVG 直出 | LLM 对 TikZ 训练数据量碾压其它方案 |
| 06-24 | 代码空间修改非像素空间 | 多轮修改一致性是核心需求 |
| 06-24 | 腾讯云 COS 而非 Vercel | 国内访问速度 |
| 06-24 | 单文件 HTML | 最快 MVP 路径 |
| 06-24 | Node 服务代理模型调用 | 避免浏览器暴露 API Key，满足真实用户开箱使用 |
| 06-24 | 内置高频模板优先 | 需求示例需要确定性精确输出，不能完全依赖 LLM |
| 06-24 | 中文 SVG 预览 + TikZ 导出 | node-tikzjax 对中文标注不稳定，产品预览先用原生 SVG 闭环 |
| 06-24 | 扩展立体几何和统计模板 | 覆盖高中截面图和教辅统计图的基础试用场景 |
| 06-24 | 首页一键示例入口 | 覆盖常见图形和辅导修改链，老师不用先记忆提示词格式 |
| 06-24 | 服务端静态资源白名单 | 保护服务端模型配置路径，避免公开仓库和服务端源码文件 |
| 06-24 | 静态页本地模板 fallback | COS 页面无 `/api` 时仍可直接演示内置模板，贴近无需安装的首次体验 |
| 06-24 | 角标注修改模板 | 覆盖辅导学生场景中的“把这个角标出来”连续修改需求 |
| 06-24 | 退役浏览器端 TikZJax 占位脚本 | 当前没有可交付的浏览器 bundle；静态模板走原生 SVG，模板外图形交给服务端 node-tikzjax 兜底 |
| 06-24 | 直角标记外移一键入口 | 覆盖产品需求中的“把直角标记从内侧移到外侧”连续修改示例 |
| 06-24 | 条形统计图解析提示词人数 | 统计图不再只画固定示例数据，能按老师输入的优秀、良好、及格、待提高人数生成 |
| 06-24 | 泛化直角三角形边长移除 | “去掉边长标注，加斜边高”不再只适用于 3-4-5，也适用于任意直角边模板 |
| 06-24 | 修复直角标记外移后的斜边高链式修改 | 先把直角标记移到外侧，再去边长并加斜边高时，两个修改都会保留 |
| 06-24 | 修复直角标记外移后的辅助线链式修改 | 先把直角标记移到外侧，再加辅助线时，两个修改都会保留 |
| 06-24 | 内切圆等腰三角形模板支持半径参数 | 覆盖 README 首屏承诺示例，并支持老师输入不同内切圆半径 |
| 06-24 | 圆内接正多边形支持边数参数 | 不再只支持正六边形，能按“正五边形”等中文描述生成对应顶点 |
| 06-24 | 扩展常见中文输入解析 | 直角边支持“6、8”等中文标点分隔，坐标范围支持“-6 至 6”等表达，降低老师首次输入的格式负担 |
| 06-24 | 补齐正十一边形中文边数解析 | 圆内接正多边形模板覆盖三到十二边的中文说法，不再漏掉“正十一边形” |
| 06-24 | 支持中文函数名触发三角函数模板 | 老师可输入“正弦函数和余弦函数”，不必记住英文 `sin/cos` 写法 |
| 06-24 | 支持“x平方”触发交点阴影模板 | 做课件场景中老师可用中文描述二次函数，不必输入 `x²` 或 `x^2` |
| 06-24 | 归一化全角加号输入 | 中文输入法下的 `2x＋1` 可按 `2x+1` 识别，避免函数模板因符号宽度失配而回退 |
| 06-24 | 斜边高修改支持省略“标注” | “去掉边长，加一条斜边上的高”也能保持坐标并移除边长标签，贴近日常课堂口述 |
| 06-24 | 直角边参数支持省略“分别” | “两条直角边为 7 和 24”也能解析为精确 7-24-25 直角三角形 |
| 06-24 | 条形统计图人数单位可省略 | “优秀11，良好17，及格9，待提高2”会按输入数据生成，不再回退默认人数 |
| 06-24 | 角标注支持数学符号写法 | “标出∠B”也能触发角 B 标注，适配老师在题干中常用的角符号 |
| 06-24 | 坐标范围修改支持省略“坐标系” | “把范围改成 -4 到 4”也会保持原函数曲线并更新坐标轴范围 |
| 06-24 | 归一化全角减号输入 | 中文输入法下的 `－5 到 5` 可按 `-5 到 5` 解析，避免坐标范围退化成 `5 到 5` |
| 06-24 | 边长移除支持倒装表达 | “把边长去掉，加一条斜边上的高”会按连续修改处理，保持原三角形坐标不变 |
| 06-24 | 首页补齐坐标范围修改入口 | 生成函数图像后可直接点“范围 -5 到 5”完成需求示例中的坐标范围修改 |
| 06-24 | 直角标记外移支持口述“外面” | “把直角标记移到外面”也会按外移处理，保持三角形坐标和边长标注不变 |
| 06-24 | 交点阴影支持“x 的平方”表达 | “y = x 的平方 和 y = 2x + 1”也会触发交点区域阴影模板，贴近老师口述 |
| 06-24 | 直角边参数支持“直角边长”表达 | “两条直角边长分别是 8 和 15”会生成精确 8-15-17 直角三角形，不再回退默认 3-4-5 |
| 06-24 | 辅助线修改支持“画一条辅助线” | 辅导场景中“画一条辅助线”也会按连续修改处理，保持原三角形坐标不变 |
| 06-24 | 圆内接多边形支持省略“正” | “圆内接六边形”会按正六边形生成，降低老师首次输入的格式要求 |
| 06-24 | 补齐 COS 发布命令入口 | `npm run deploy:cos` 调用静态资源发布脚本，和 `npm run verify:deployment` 形成发布后复验路径 |
| 06-24 | COS 发布预检先校验凭据 | 缺少腾讯云凭据时先给出明确错误，不再提前导入 SDK 产生无关环境警告 |
| 06-24 | COS 发布后默认执行内容验证 | `deploy_cos.py` 上传后会运行 `npm run verify:deployment`，避免只凭 HTTP 200 误判旧页面已更新 |
| 06-24 | 坐标范围支持区间写法 | 初次生成和连续修改都支持 `[-5, 5]` / `[-4, 4]` 这类数学区间输入 |
| 06-24 | 条形统计图支持中文冒号数据 | “优秀：13人、良好：15人”会按输入人数生成，不再回退默认数据 |
| 06-24 | 交点阴影支持“围成的区域” | “y=x平方 和 y=2x+1 围成的区域”会触发交点区域阴影模板，贴近课件口述 |
| 06-24 | 交点阴影支持中文“加”写法 | “y=x平方 和 y=2x加1”会按 `2x+1` 识别，且不影响“添加辅助线”等修改指令 |
| 06-24 | 三角函数支持自定义 π 定义域 | “在 -π 到 π 的图像”会生成 `xmin=-pi`、`domain=-pi:pi`，SVG 刻度保留 `±π` 标签，且 y 轴保持正弦/余弦默认范围 |
| 06-24 | 等腰三角形支持底角和腰长参数 | “底角 45°、腰长 6cm”会按三角函数计算底边和高度，不再只支持 30°/5cm |
| 06-24 | 直角三角形支持边名记号输入 | “AB=6，AC=8，∠A=90°”会识别为两条直角边，生成精确 6-8-10 直角三角形 |
| 06-24 | 直角三角形支持口语边名输入 | “AB长6厘米，AC长8厘米，角A是90度”会按边名和直角位置解析为 6-8-10 直角三角形 |
| 06-24 | 角标注支持指定角 C | 连续修改中“标出∠C”会在 C 点添加角弧和 θ 标注，不再只支持角 B |
| 06-24 | 角标注支持指定角 A | 连续修改中“标出∠A”会给直角添加明确 `90^\circ` 标注，满足精确角度标注需求 |
| 06-24 | 首页补齐角 A/C 标注入口 | 快捷修改按钮覆盖“标出∠A / 标出∠C”，和角 B 一起支持课堂连续修改演示 |
| 06-25 | 修改按钮按图形类型启用 | 函数图只开放坐标范围修改，直角三角形只开放几何修改，避免 happy path 中误点不适用修改后进入模板外提示 |
| 06-25 | 支持等腰三角形 ABC 与异侧点 P 题图 | 识别“AB=AC、P/A 位于 BC 异侧、∠PBC=∠BAC、∠APB+2∠PAB=90°、BC=8、PB=5”的输入，生成满足关系的标准构型 |
| 06-26 | 增加测试数据集评估脚本 | `npm run evaluate:dataset` 从 `docs/测试数据集.md` 解析 38 个样例，报告单轮/多轮模板覆盖和误匹配，作为数据集回归入口 |
| 06-26 | 补齐基础几何与单函数模板 | 支持等边三角形、正方形、单圆半径/圆心、底边+腰长等腰三角形、直角三角形内切圆、一次函数、二次函数、单独正弦函数、参数椭圆、通用柱状图 |
| 06-26 | 收紧模板匹配避免错误出图 | 相交圆、坐标轴圆、分段函数、抛物线与圆交点等复杂请求不再被单圆/单抛物线模板误接；非直角三角形不再套用直角三角形修改器 |
| 06-26 | 首页补齐数据集支撑示例 | 一键示例新增等边三角形、正方形、单圆、底腰等腰、直角内切圆、一次/二次/正弦函数、参数椭圆、通用柱状图，并纳入部署复验 |
| 06-26 | 增加 Node 服务验收脚本 | `/healthz` 暴露不含密钥的服务状态，`npm run verify:service` 可验证本地或公网 Node 服务的首页、健康检查和 `/api/generate` happy path |
| 06-26 | 收紧 COS 发布完成提示 | `deploy_cos.py` 不再提示默认 COS URL 可直接分享，改为要求先通过 `npm run verify:deployment`，并提示使用自定义域名或 Node 服务 URL 做浏览器预览 |
| 06-26 | 增加 Node 容器部署入口 | `Dockerfile` 默认监听 `0.0.0.0:3000`，镜像只复制运行必需文件；`docs/Node服务部署.md` 记录 Docker 运行、模型环境变量和公网验收命令 |
| 06-26 | 补齐数据集多轮修改链 | 支持二次+一次函数同图生成和样式修改、等腰三角形边长改角度、圆内接六边形改八边形、正弦函数保留第一象限，使测试数据集多轮 #34-38 全部命中 |
| 06-26 | 复验本地 Node 服务 happy path | `TALK2GRAPH_SERVICE_URL=http://127.0.0.1:3001 npm run verify:service` 通过；Docker 构建在本机停在 `node:22-alpine` 元数据拉取阶段，尚未形成容器运行验收结论 |
| 06-26 | COS 上传对象头显式内联和禁用缓存 | `deploy_cos.py` 上传 HTML/JS 时设置 `Content-Disposition: inline` 和 `Cache-Control: no-cache, no-store, must-revalidate`，减少旧首页和下载行为由对象元数据引起的风险；真实修复仍以发布后 `npm run verify:deployment` 为准 |
| 06-26 | 增加非 Docker Node 服务发布包 | `npm run package:service` 生成 `dist/talk2graph-service.tar.gz`，包含运行必需文件和 systemd/nginx 模板，规避 Docker Hub 基础镜像拉取不稳定时的公网 Node 部署阻塞 |
| 06-26 | 扩展测试数据集模板覆盖到 31/38 | 新增平行四边形、直角梯形、已知三边三角形、独立正多边形、三角形中线、坐标三角形、坐标线段中点、坐标圆四交点、百分比饼图、频率分布直方图等模板；多轮仍保持 5/5 命中 |
| 06-26 | 补齐测试数据集到 38/38 | 新增相交圆、log₂ 函数、三次函数极值和零点、分段函数、三角形中垂线与外心、抛物线和圆交点区域、关于 y=x 的对称点模板，数据集单轮 33/33、多轮 5/5、语义误匹配 0 |
| 06-26 | 复验当前交付门禁 | `npm run evaluate:dataset` 仍为 38/38，`TALK2GRAPH_SERVICE_URL=http://127.0.0.1:3002 npm run verify:service` 通过；默认 COS 地址 `npm run verify:deployment` 仍因强制下载和线上首页缺数据集一键示例失败 |
| 06-26 | COS 发布支持隐藏输入凭据 | `npm run deploy:cos -- --prompt-credentials` 会通过终端隐藏输入 SecretId/SecretKey，避免把密钥写进命令行、shell 历史或项目文件 |
| 06-27 | 增加直角三角形直径圆题图模板 | 支持“C 为直角顶点，AC=3，BC=4；以 AC 为直径画圆，圆与斜边 AB 相交于点 D”的确定性生成，不再依赖服务端模型和 TikZJax 兜底渲染 |
| 06-27 | 服务验收覆盖直径圆题图模板 | `npm run verify:service` 不再只检查基础直角三角形，会同时确认该题图返回确定性模板，避免旧公网进程仍走 LLM 时被误判可验收 |
| 06-27 | 健康检查暴露安全版本信息 | `/healthz` 返回 `version` 和 `commit`，发布包生成 `build-info.json`，`npm run verify:service` 拒绝 `unknown` 版本信息，用于定位公网是否仍在运行旧包且不暴露任何密钥 |
| 06-27 | 增加 CVM 安装脚本 | 发布包携带 `deploy/install-cvm.sh`，服务器上可从 tar 包直接执行安装并重启 systemd 服务，同时检查 `/healthz` 提交号和直径圆题图模板，减少手工部署跑旧包的风险 |
